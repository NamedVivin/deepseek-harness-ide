/**
 * IDE surface plugin, node half. The empty apply lets the Host Loader roster
 * discover and serve the browser bundle declared by `dsh.client`.
 */

/** Host plugin body; all product behavior lives in the Client half. */
export function apply(): void {}
