{
  "steps": [
    { "op": "initialize" },
    { "op": "newSession" },
    {
      "op": "prompt",
      "text": "Run true once with bash in the foreground. After that fails, run true with bash in the background. After that also fails, reply with exactly RUNNER_FAILURES_SURFACED and stop."
    }
  ]
}
